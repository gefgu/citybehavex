"""CityBehavEx web backend.

FastAPI service that turns the parquet outputs of a simulation into JSON plot
data for the React/Vite frontend in ``web/frontend`` — the interactive
replacement for the standalone comparison ``.html`` report.
"""

[package]
name = "cityview"
version = "0.1.0"
edition = "2024"

[dependencies]
bevy = "0.14"
flatgeobuf = { version = "6", default-features = false }
geozero = { version = "0.15", default-features = false, features = ["with-geo"] }
geo-types = "0.7"
image = "0.25"
